
import type { ColorProps } from "../../lib/data";

export const getColorProps = (_accountIdArg?: bigint): ColorProps | null => {
  return {
  "primaryColor": "#8ca28c",
  "secondaryColor": "#e2c964"
} as ColorProps | null;
}
